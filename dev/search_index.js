var documenterSearchIndex = {"docs":
[{"category":"section","location":"regridding/#Dyadic-regridding","page":"Dyadic regridding","text":"","title":"Dyadic regridding"},{"category":"section","location":"regridding/#Concept","page":"Dyadic regridding","text":"Coupled Kryos modules often run at different resolutions — an ice sheet at 1 km, isostasy at 2 km, a surface model at 4 km — and exchange fields every time step. KryosTools moves fields between such grids conservatively, without allocating temporaries, on CPUs and GPUs.\n\nIt relies on the grids being dyadic:\n\neach grid is regular and isotropic (dx == dy), described by a DyadicGrid built from the cell-centre coordinates a module already has;\ntwo grids are paired when their spacings differ by a power of 2 and their corners coincide.\n\nEvery coarse cell then contains a whole number of fine cells with perfect overlap, as in the sketch below: nothing needs to be interpolated between arbitrary points, and no weights or index lists need to be stored. Note that \"all resolutions are multiples of 500 m\" is not enough — 1.5 km and 2 km cells do not nest.\n\n(Image: Coarsening averages the fine cells of a coarse cell; refinement reconstructs them.)\n\nThe two directions are not symmetric.\n\nCoarsening (AverageCoarsening) assigns each coarse cell the mean of the fine cells it contains. This is the exact cell integral, so there is nothing to choose. An optional weight — typically the true cell area on a distorted projection, or a mask — turns it into a weighted mean that conserves Σ wᵢxᵢ.\nRefinement has to reconstruct sub-cell detail, which is a modelling choice. ConstantRefinement copies the coarse value into every fine cell. LinearRefinement reconstructs T_c + Σ_d s_d ξ_d within each coarse cell, with slopes s_d from the neighbouring cells, and gives each fine cell the mean of that reconstruction.\n\nRefinement is conservative whatever the slopes are. The offset of child j of r from its parent's centre is w_j = (2j − 1 − r) / 2r, and these offsets sum to zero. The fine values therefore always average back to T_c, as the lower part of the sketch shows. Coarsening a refined field returns the original exactly. A slope limiter (MinModLimiter) can hence bound overshoot — for instance to avoid negative thickness next to an ice margin — without affecting conservation, although it makes the operator non-linear.\n\nIn the outermost coarse cells the neighbour needed for a centred slope is missing, so the slope along that axis is zero there and LinearRefinement falls back to copying.\n\nWhen two modules share a grid, IdentityRegridding makes the exchange a plain copy.\n\nA coupler usually needs both directions between two grids, which is what BidirectionalRegridding provides. It picks the right one-way regridder for each direction from the grids, and chooses the direction of each call from the sizes of the two fields.\n\nAbstractRegridding\n├── AbstractCoarsening\n│   └── AverageCoarsening\n├── AbstractRefinement\n│   ├── ConstantRefinement\n│   └── LinearRefinement\n├── IdentityRegridding\n└── BidirectionalRegridding\n\nA few practical points:\n\nRegridders are built once, at setup. Their concrete types depend on the grids, so store them in parametric fields; regrid! itself is type-stable and does not allocate memory proportional to the fields. KernelAbstractions adds a small constant overhead per call on the CPU.\nOne regridder serves fields of any floating-point type. Float16 fields are accumulated in Float32.\nOn GPUs, regrid! is asynchronous like any KernelAbstractions kernel. GPU and CPU results agree to rounding: the GPU compiler may fuse multiply-add operations.","title":"Concept"},{"category":"section","location":"regridding/#Example","page":"Dyadic regridding","text":"Two grids covering the same 8 km × 8 km domain, at 1 km and 2 km:\n\njulia> using KryosTools\n\njulia> x1 = 500.0:1000.0:7500.0;  # cell centres at 1 km\n\njulia> x2 = 1000.0:2000.0:7000.0; # cell centres at 2 km\n\njulia> g1, g2 = DyadicGrid(x1, x1), DyadicGrid(x2, x2);\n\njulia> rgd = BidirectionalRegridding(g1, g2)\nBidirectionalRegridding(8×8 ↔ 4×4: to1 = LinearRefinement, to2 = AverageCoarsening)\n\nRefine a field from grid 2 onto grid 1. Along each axis, the interior coarse cells get linear profiles, while the outermost ones are copied:\n\njulia> h2 = [100.0 * i for i in 1:4, j in 1:4];\n\njulia> h1 = zeros(8, 8);\n\njulia> regrid!(h1, h2, rgd);\n\njulia> h1[:, 1]\n8-element Vector{Float64}:\n 100.0\n 100.0\n 175.0\n 225.0\n 275.0\n 325.0\n 400.0\n 400.0\n\nThe refinement is conservative, and coarsening it back returns the original field:\n\njulia> sum(h1) == 4 * sum(h2)\ntrue\n\njulia> regrid(h1, rgd) == h2\ntrue","title":"Example"},{"category":"section","location":"#KryosTools","page":"Home","text":"Shared tools for the Kryos family of ice-sheet and boundary-condition models.\n\nDyadic regridding: conservative regridding between nested grids.","title":"KryosTools"},{"category":"section","location":"#API-reference","page":"Home","text":"","title":"API reference"},{"category":"type","location":"#KryosTools.AbstractCoarsening","page":"Home","text":"AbstractCoarsening{N,L}\n\nA regridding from a finer grid1 to a coarser (or equal) grid2.\n\n\n\n\n\n","title":"KryosTools.AbstractCoarsening"},{"category":"type","location":"#KryosTools.AbstractGrid","page":"Home","text":"AbstractGrid{N,T}\n\nAn N-dimensional grid with coordinates of type T.\n\n\n\n\n\n","title":"KryosTools.AbstractGrid"},{"category":"type","location":"#KryosTools.AbstractLimiter","page":"Home","text":"AbstractLimiter\n\nSlope limiter for LinearRefinement. A limiter replaces the centred slope with one that keeps the reconstruction within the range of the neighbouring coarse values, so that refinement creates no new extrema (and, for instance, no negative thickness).\n\nLimiting changes only the slopes, and the refinement is conservative whatever the slopes are, so limited refinement is still exactly conservative. It is no longer linear, though, and limiters are not differentiable where they switch branches: gradients through a limited refinement are zero wherever the limiter clips the slope, for example at ice divides and sharp margins. Limiters are written without branches, so they run on GPUs and trace under compilers such as Reactant.\n\n\n\n\n\n","title":"KryosTools.AbstractLimiter"},{"category":"type","location":"#KryosTools.AbstractLocation","page":"Home","text":"AbstractLocation\n\nWhere a field lives within a grid cell. Only Center is implemented so far; the other locations are declared so that staggered (Arakawa C-grid) fields can be added without an API break.\n\n\n\n\n\n","title":"KryosTools.AbstractLocation"},{"category":"type","location":"#KryosTools.AbstractRefinement","page":"Home","text":"AbstractRefinement{N,L}\n\nA regridding from a coarser grid1 to a finer (or equal) grid2.\n\n\n\n\n\n","title":"KryosTools.AbstractRefinement"},{"category":"type","location":"#KryosTools.AbstractRegridding","page":"Home","text":"AbstractRegridding{N,L}\n\nAn operator that moves N-dimensional fields located at L from grid1 to grid2, applied with regrid!. Every concrete subtype provides:\n\nregrid!(dst, src, rgd), writing into dst without allocating;\nthe fields grid1 and grid2, as returned by grids;\nratio.\n\nRegridders hold no scratch space and are built once, at setup.\n\n\n\n\n\n","title":"KryosTools.AbstractRegridding"},{"category":"type","location":"#KryosTools.AverageCoarsening","page":"Home","text":"AverageCoarsening(grid1, grid2; weights = nothing, location = Center())\n\nCoarsening from grid1 onto a coarser grid2: each coarse value is the mean of the fine cells it contains. This is the exact cell integral, so it is conservative and needs no scheme.\n\ngrid1 must be finer than or equal to grid2, by a power-of-2 ratio, with coincident corners. For identical grids the operator is correct, but IdentityRegridding is the fast path.\n\nWeights\n\nWith weights — a non-negative array on grid1 — each coarse value is the weighted mean Σ wᵢxᵢ / Σ wᵢ over its block, which conserves Σ wᵢxᵢ. Typical weights are true cell areas (weights = grid1.area, for area conservation on a distorted projection) or a mask (zeros exclude cells). Constant factors cancel. Every coarse cell needs a positive total weight. Weights are stored as Float64 on the device of the array passed in, and must live on the same backend as the fields. They are never applied implicitly.\n\n\n\n\n\n","title":"KryosTools.AverageCoarsening"},{"category":"type","location":"#KryosTools.BidirectionalRegridding","page":"Home","text":"BidirectionalRegridding(grid1, grid2; refinement = LinearRefinement, weights = nothing,\n                        location = Center(), refinement_kwargs...)\n\nRegridding in both directions between two grids, as a coupler needs it: regrid!(x2, x1, rgd) writes onto grid2 and regrid!(x1, x2, rgd) onto grid1. The direction is detected from the sizes of both fields.\n\nThe grids may be passed in either order. For each direction the fitting one-way regridder is built — AverageCoarsening towards the coarser grid, a refinement towards the finer one, and IdentityRegridding (a plain copy) if the grids are identical — and stored in the fields to1 and to2, named after their destination grid.\n\nrefinement is a refinement type; any further keyword (e.g. limiter = MinModLimiter()) is passed to its constructor.\nweights belong to the finer grid, whichever number it has, and only affect coarsening.\n\nCoarsening undoes refinement exactly, but not the other way round: refining a coarsened field does not restore the fine field.\n\nWhich regridders are stored depends on the grid spacings, so the constructor's return type cannot be inferred. Build it once, at setup, and store it in a parametric field; regrid! itself is type-stable.\n\n\n\n\n\n","title":"KryosTools.BidirectionalRegridding"},{"category":"type","location":"#KryosTools.Center","page":"Home","text":"Cell-centred field, such as ice thickness or temperature.\n\n\n\n\n\n","title":"KryosTools.Center"},{"category":"type","location":"#KryosTools.ConstantRefinement","page":"Home","text":"ConstantRefinement(grid1, grid2; location = Center())\n\nRefinement from grid1 onto a finer grid2 that copies each coarse value into all the fine cells it covers. Exactly conservative, and exactly undone by AverageCoarsening; blocky.\n\ngrid2 must be finer than or equal to grid1, by a power-of-2 ratio, with coincident corners.\n\n\n\n\n\n","title":"KryosTools.ConstantRefinement"},{"category":"type","location":"#KryosTools.Corner","page":"Home","text":"Field on cell corners. Declared, not yet implemented.\n\n\n\n\n\n","title":"KryosTools.Corner"},{"category":"type","location":"#KryosTools.DyadicGrid","page":"Home","text":"DyadicGrid(x, y; area = nothing)\nDyadicGrid(coords::AbstractVector...; area = nothing)\n\nA regular, isotropic Cartesian grid built from the cell-centre coordinate vectors that a coupled module already has.\n\n\"Dyadic\" refers to how such grids are paired: two grids can be regridded onto each other when their spacings differ by a power of 2 and their corners coincide, so that every coarse cell contains a whole number of fine cells with perfect overlap. Those are properties of a pair and are checked when a regridder is built. A single grid is validated for what it can know about itself:\n\nat least 2 points per axis;\nstrictly increasing coordinates (descending axes throw — flipping an axis also means flipping the data, which is the caller's job);\nuniform spacing, and the same spacing along every axis.\n\nCoordinates may be of any real type and may live on a GPU. Validation allows for the rounding of the input type — Float32 coordinates at several thousand kilometres are accepted — and the grid then stores exact Float64 ranges fitted to the input. centers(g) may therefore differ from the vectors passed in by rounding noise.\n\nCell area\n\narea, if given, is the true area of each cell in the coordinate unit squared (m² for metre coordinates), for example to account for the distortion of a polar stereographic projection. It must match the grid size and be strictly positive. It is stored as Float64 on the device of the array passed in.\n\nThe area is never used implicitly. To coarsen area-conservatively, pass it explicitly as weights. Since regridding only uses relative areas, any constant factor cancels.\n\nA warning is emitted if the mean of area / spacing^N lies outside [0.5, 2]. That usually means a dimensionless distortion factor was passed where true areas were expected.\n\nExamples\n\njulia> x = 500.0:1000.0:7500.0;  # 8 cell centres, 1 km apart\n\njulia> g = DyadicGrid(x, x)\n8×8 DyadicGrid with spacing 1000.0 and origin (0.0, 0.0)\n\njulia> spacing(g), size(g)\n(1000.0, (8, 8))\n\n\n\n\n\n","title":"KryosTools.DyadicGrid"},{"category":"type","location":"#KryosTools.IdentityRegridding","page":"Home","text":"IdentityRegridding(grid1, grid2; location = Center())\n\nRegridding between two geometrically identical grids: a plain copyto!. Throws unless the grids have the same size, spacing and origin; cell areas are ignored.\n\n\n\n\n\n","title":"KryosTools.IdentityRegridding"},{"category":"type","location":"#KryosTools.LinearRefinement","page":"Home","text":"LinearRefinement(grid1, grid2; limiter = nothing, location = Center())\n\nRefinement from grid1 onto a finer grid2 by conservative piecewise-linear reconstruction. Within each coarse cell the field is T_c + Σ_d s_d ξ_d, where ξ_d is the position within the cell in units of the coarse spacing (from -1/2 to 1/2) and s_d is the centred difference of the neighbouring coarse values. Each fine cell receives the mean of that reconstruction over its extent.\n\nThe fine-cell offsets within a coarse cell sum to zero, so the block mean of the refined field is exactly T_c whatever the slopes are: the scheme is conservative and exactly undone by AverageCoarsening. It reproduces fields that are linear in each coordinate; it has no cross term, so bilinear fields are not reproduced.\n\nBoundaries\n\nThe centred slope needs both neighbours, so in the outermost coarse cells along each axis the slope is zero and the scheme reduces to ConstantRefinement along that axis.\n\nLimiter\n\nWith limiter = nothing (the default) the operator is exactly linear in the field. The reconstruction can then overshoot near sharp gradients — e.g. produce negative ice thickness next to a margin. A limiter (see AbstractLimiter) bounds the overshoot at the cost of linearity; conservation is unaffected.\n\ngrid2 must be finer than or equal to grid1, by a power-of-2 ratio, with coincident corners.\n\n\n\n\n\n","title":"KryosTools.LinearRefinement"},{"category":"type","location":"#KryosTools.MinModLimiter","page":"Home","text":"MinModLimiter()\n\nThe minmod limiter: the one-sided difference of smaller magnitude when both have the same sign, and zero at local extrema. The most diffusive classic limiter, and the one that bounds the reconstruction most strictly.\n\n\n\n\n\n","title":"KryosTools.MinModLimiter"},{"category":"type","location":"#KryosTools.XFace","page":"Home","text":"Field on x-normal cell faces. Declared, not yet implemented.\n\n\n\n\n\n","title":"KryosTools.XFace"},{"category":"type","location":"#KryosTools.YFace","page":"Home","text":"Field on y-normal cell faces. Declared, not yet implemented.\n\n\n\n\n\n","title":"KryosTools.YFace"},{"category":"method","location":"#KryosTools.cellarea-Tuple{DyadicGrid, Vararg{Any}}","page":"Home","text":"cellarea(g::DyadicGrid, I...)\n\nTrue area of cell I: spacing(g)^N when the grid was built without area, and the stored area otherwise. A host-side convenience; it indexes the area array directly.\n\n\n\n\n\n","title":"KryosTools.cellarea"},{"category":"method","location":"#KryosTools.centers-Tuple{DyadicGrid}","page":"Home","text":"centers(g::DyadicGrid)\n\nCell-centre coordinates along each axis, as a tuple of ranges.\n\n\n\n\n\n","title":"KryosTools.centers"},{"category":"method","location":"#KryosTools.faces-Tuple{DyadicGrid}","page":"Home","text":"faces(g::DyadicGrid)\n\nCell-face coordinates along each axis, as a tuple of ranges one element longer than centers.\n\n\n\n\n\n","title":"KryosTools.faces"},{"category":"method","location":"#KryosTools.grids-Tuple{AbstractRegridding}","page":"Home","text":"grids(rgd) -> (grid1, grid2)\n\nThe two grids of a regridder, in the order they were passed.\n\n\n\n\n\n","title":"KryosTools.grids"},{"category":"method","location":"#KryosTools.isconservative-Tuple{AbstractRegridding}","page":"Home","text":"isconservative(rgd)\n\nWhether rgd preserves the integral of the field. True for every regridder in KryosTools.\n\n\n\n\n\n","title":"KryosTools.isconservative"},{"category":"method","location":"#KryosTools.islinear-Tuple{IdentityRegridding}","page":"Home","text":"islinear(rgd)\n\nWhether rgd is a linear map of the field. Linear regridders have exact, cheap adjoints, so gradients through them are exact.\n\n\n\n\n\n","title":"KryosTools.islinear"},{"category":"method","location":"#KryosTools.ratio-Union{Tuple{AverageCoarsening{N, L, R, G1, G2} where {G1<:(DyadicGrid{N}), G2<:(DyadicGrid{N})}}, Tuple{R}, Tuple{L}, Tuple{N}} where {N, L, R}","page":"Home","text":"ratio(rgd)\n\nSpacing ratio between the coarser and the finer grid of rgd: a power of 2, and 1 for identical grids.\n\n\n\n\n\n","title":"KryosTools.ratio"},{"category":"function","location":"#KryosTools.regrid!","page":"Home","text":"regrid!(dst, src, rgd) -> dst\n\nRegrid the field src, defined on grid1 of rgd, onto grid2, writing into dst. Allocates nothing. src and dst may have different element types, but must live on the same KernelAbstractions backend.\n\nLike any KernelAbstractions kernel, the call is asynchronous on GPUs: call KernelAbstractions.synchronize(backend) before timing it or reading dst on the host.\n\n\n\n\n\n","title":"KryosTools.regrid!"},{"category":"method","location":"#KryosTools.regrid-Tuple{AbstractArray, AbstractRegridding}","page":"Home","text":"regrid(src, rgd) -> dst\n\nAllocating form of regrid!: returns a new array on grid2 with the element type and backend of src. A convenience; use regrid! in hot loops.\n\n\n\n\n\n","title":"KryosTools.regrid"},{"category":"method","location":"#KryosTools.spacing-Tuple{DyadicGrid}","page":"Home","text":"spacing(g::DyadicGrid)\n\nCell size, identical along every axis.\n\n\n\n\n\n","title":"KryosTools.spacing"}]
}
